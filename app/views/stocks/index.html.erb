<div id="stocks-index">
  <h2>Your Portfolio</h2>
  <br>
  <%= link_to "Add Stock", new_stock_path, class: "btn btn-default" %>
  <br>

  <div class="stock-link">
    <% @stocks.each do |stock| %>
    <br>
    <h4><%= link_to "#{stock.symbol} ● #{number_to_currency(current_price(stock))}", stock_path(stock) %></h4>
    <% end %>
  </div>
</div>
